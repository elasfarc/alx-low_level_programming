#include <stdio.h>

/**
 * main - entry point, prints str
 *
 * Return: 0
 **/
int main(void)
{
	char *str = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}

