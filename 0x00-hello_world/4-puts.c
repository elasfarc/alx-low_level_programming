#include <stdio.h>

/**
 * main - entry point, prints the string str
 *
 * Return: 0
 */
int main(void)
{
	char *str = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}

